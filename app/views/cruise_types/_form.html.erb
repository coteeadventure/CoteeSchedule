<%= form_for :cruise_type, url: cruise_type_path(@cruise_type), method: :patch do |f| %>
 
  <% if @cruise_type.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@cruise_types.errors.count, "error") %> prohibited
        this Cruise Type from being saved:
      </h2>
      <ul>
        <% @cruise_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :capacity %><br>
    <%= f.text_field :capacity %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
