<%= form_for :scheduled_cruise, url: scheduled_cruises_path do |f| %>

  <% if @scheduled_cruise.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@scheduled_cruise.errors.count, "error") %> prohibited
        this Cruise Type from being saved:
      </h2>
      <ul>
        <% @scheduled_cruise.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :cruise %><br>
	<%= f.collection_select :cruise_type_id, CruiseType.all, :id, :name %>
  </p>
 
  <p>
    <%= f.label :date_and_time %><br>
    <%= f.text_field :datetime %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
  
<% end %>
